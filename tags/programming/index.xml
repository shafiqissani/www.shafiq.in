<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Shafiq Alibhai </title>
    <link>https://shafiqissani.github.io/www.shafiq.in/tags/programming/</link>
    <language>en-us</language>
    <author></author>
    <rights>(C) 2016</rights>
    <updated>2013-06-07 10:52:57 &#43;0000 UTC</updated>

    
      
        <item>
          <title>Online Temperature Monitoring System</title>
          <link>https://shafiqissani.github.io/www.shafiq.in/2013/06/07/online-temperature-monitoring-system/</link>
          <pubDate>Fri, 07 Jun 2013 10:52:57 UTC</pubDate>
          <author></author>
          <guid>https://shafiqissani.github.io/www.shafiq.in/2013/06/07/online-temperature-monitoring-system/</guid>
          <description>&lt;h1&gt;Download : &lt;a href=&#34;http://shafiqissani.files.wordpress.com/2013/06/proj_format_.doc&#34;&gt;PROJ_FORMAT_&lt;/a&gt;&lt;/h1&gt;
&lt;p&gt;Contents Page number&lt;/p&gt;
&lt;p&gt;1. Introduction&lt;br /&gt;
2. Working Environment&lt;br /&gt;
2.1 Hardware Requirements specifications&lt;br /&gt;
2.2 About the Hardware&lt;br /&gt;
2.3 Software Requirements specifications&lt;br /&gt;
2.4 About the Software&lt;br /&gt;
3. Hardware Design And Description&lt;br /&gt;
4. System Analysis&lt;br /&gt;
4.1 &lt;a class=&#34;zem_slink&#34; title=&#34;Data flow diagram&#34; href=&#34;http://en.wikipedia.org/wiki/Data_flow_diagram&#34; target=&#34;_blank&#34; rel=&#34;wikipedia&#34;&gt;Data Flow Diagram&lt;/a&gt;&lt;br /&gt;
4.2 Control Flow Diagram&lt;br /&gt;
5. Software Design&lt;br /&gt;
5.1 Flow chart&lt;br /&gt;
5.2 System Implementation&lt;br /&gt;
6. Message Transaction Protocols&lt;br /&gt;
6.1 1-Wire &lt;a class=&#34;zem_slink&#34; title=&#34;Communications protocol&#34; href=&#34;http://en.wikipedia.org/wiki/Communications_protocol&#34; target=&#34;_blank&#34; rel=&#34;wikipedia&#34;&gt;Communication Protocol&lt;/a&gt;&lt;br /&gt;
6.2 I2C protocol&lt;br /&gt;
6.3 &lt;a class=&#34;zem_slink&#34; title=&#34;Serial communication&#34; href=&#34;http://en.wikipedia.org/wiki/Serial_communication&#34; target=&#34;_blank&#34; rel=&#34;wikipedia&#34;&gt;Serial Communication&lt;/a&gt; Protocol&lt;br /&gt;
7. Operational Specifications&lt;br /&gt;
8. Application Real-Time&lt;br /&gt;
9. Conclusion&lt;br /&gt;
9.1 Future Scope&lt;br /&gt;
9.2 Limitations&lt;br /&gt;
10. Bibliography&lt;/p&gt;
&lt;p&gt;1. Introduction&lt;/p&gt;
&lt;p&gt;2. Working environment&lt;/p&gt;
&lt;p&gt;2.1 Hardware Requirement Specification&lt;/p&gt;
&lt;p&gt;i. Microcontroller AT89c52&lt;br /&gt;
ii. ADC ADC0808&lt;br /&gt;
iii. &lt;a class=&#34;zem_slink&#34; title=&#34;Real-time clock&#34; href=&#34;http://en.wikipedia.org/wiki/Real-time_clock&#34; target=&#34;_blank&#34; rel=&#34;wikipedia&#34;&gt;Real Time Clock&lt;/a&gt; DS1307&lt;br /&gt;
iv. Serial EEPROM AT24c08&lt;br /&gt;
v. Serial Transceiver MAX232&lt;br /&gt;
vi. LCD 16x2&lt;/p&gt;
&lt;p&gt;2.2 &lt;a class=&#34;zem_slink&#34; title=&#34;Requirement&#34; href=&#34;http://en.wikipedia.org/wiki/Requirement&#34; target=&#34;_blank&#34; rel=&#34;wikipedia&#34;&gt;Software Requirement&lt;/a&gt; Specification&lt;/p&gt;
&lt;p&gt;i. Programming Language ANSI C&lt;br /&gt;
ii. Compiler KEIL&lt;/p&gt;
&lt;p&gt;2.3 About the Hardware&lt;/p&gt;
&lt;p&gt;2.3.1 Microcontroller (89C52)&lt;br /&gt;
Key Features&lt;br /&gt;
 Compatible with MCS-51™ Products&lt;br /&gt;
 8K Bytes of In-System Reprogrammable &lt;a class=&#34;zem_slink&#34; title=&#34;Flash memory&#34; href=&#34;http://en.wikipedia.org/wiki/Flash_memory&#34; target=&#34;_blank&#34; rel=&#34;wikipedia&#34;&gt;Flash Memory&lt;/a&gt;&lt;br /&gt;
 Endurance: 1,000 Write/Erase Cycles&lt;br /&gt;
 256 x 8-bit Internal RAM&lt;br /&gt;
 32 Programmable I/O Lines&lt;br /&gt;
 Three 16-bit Timer/Counters&lt;br /&gt;
 Eight Interrupt Sources&lt;br /&gt;
 Programmable Serial Channel&lt;/p&gt;
&lt;p&gt;Description&lt;br /&gt;
The AT89C52 is a low-power, high-performance CMOS 8-bit microcomputer with 8K bytes of Flash programmable and erasable read only memory (PEROM). The device is manufactured using &lt;a class=&#34;zem_slink&#34; title=&#34;NASDAQ: ATML&#34; href=&#34;http://www.google.com/finance?q=NASDAQ:ATML&#34; target=&#34;_blank&#34; rel=&#34;googlefinance&#34;&gt;Atmel&lt;/a&gt;’s high-density nonvolatile memory technology. The on-chip Flash allows the program memory to be reprogrammed in-system or by a conventional nonvolatile memory programmer. By combining a versatile 8-bit CPU with Flash on a monolithic chip, the Atmel AT89C52 is a powerful microcomputer which provides a highly-flexible and cost-effective solution to many embedded control applications.&lt;/p&gt;
&lt;p&gt;Pin Configurations&lt;/p&gt;
&lt;p&gt;Pin Description&lt;br /&gt;
• RST&lt;br /&gt;
Reset input. A high on this pin for two machine cycles while the oscillator is running resets the device.&lt;br /&gt;
• ALE/PROG&lt;br /&gt;
Address Latch Enable is an output pulse for latching the low byte of the address during accesses to external memory. This pin is also the program pulse input (PROG) during &lt;a class=&#34;zem_slink&#34; title=&#34;Adobe Flash&#34; href=&#34;http://www.adobe.com/flashplatform/&#34; target=&#34;_blank&#34; rel=&#34;homepage&#34;&gt;Flash programming&lt;/a&gt;.&lt;br /&gt;
• PSEN&lt;br /&gt;
Program Store Enable is the read strobe to external program memory. When the AT89C52 is executing code from external program memory, PSEN is activated twice each machine cycle, except that two PSEN activations are skipped during each access to external data memory.&lt;br /&gt;
• EA/VPP&lt;br /&gt;
External Access Enable. EA must be strapped to GND in order to enable the device to fetch code from external program memory locations starting at 0000H up to FFFFH.&lt;/p&gt;
&lt;p&gt;Programming the Flash&lt;/p&gt;
&lt;p&gt;To program the 89c52, take the following steps.&lt;br /&gt;
1. Input the desired memory location on the address lines.&lt;br /&gt;
2. Input the appropriate data on the data lines&lt;br /&gt;
3. Activate the correct combination of control signals&lt;br /&gt;
4. Raise EA/Vpp&lt;br /&gt;
5. Pulse ALE/PROG once to program a byte in the Flash array or lock bits. The byte-write cycle is self-timed and typically takes no more than 1.5 ms. Repeat steps 1 through 5, changing the address and data for the entire array or until the end of the object file is reached.&lt;/p&gt;
&lt;p&gt;2.3.2 ADC 0808&lt;br /&gt;
Key Features:&lt;/p&gt;
&lt;p&gt;2.3.3 Real time Clock (DS1307)&lt;br /&gt;
Features&lt;br /&gt;
 Real-time clock (RTC) counts seconds, minutes, hours, date of the month, month, day of the week, and year with leap-year compensation valid up to 2100&lt;br /&gt;
 56-byte, battery-backed, nonvolatile (NV) RAM for data storage&lt;br /&gt;
 &lt;a class=&#34;zem_slink&#34; title=&#34;I²C&#34; href=&#34;http://en.wikipedia.org/wiki/I%C2%B2C&#34; target=&#34;_blank&#34; rel=&#34;wikipedia&#34;&gt;Two-wire serial interface&lt;/a&gt;&lt;br /&gt;
 Automatic power-fail detects and switches circuitry&lt;/p&gt;
&lt;p&gt;Pin Configurations&lt;/p&gt;
&lt;p&gt;Pin Description&lt;br /&gt;
 SCL (Serial Clock Input) –SCL is used to synchronize data movement on the serial interface.&lt;br /&gt;
 SDA (Serial Data Input/Output) – SDA is the input/output pin for the 2-wire serial interface.&lt;br /&gt;
 SQW/OUT (Square Wave/Output Driver) – When enabled, the SQWE bit set to 1, the SQW/OUT pin outputs one of four square wave frequencies (1Hz, 4kHz, 8kHz, 32kHz).&lt;br /&gt;
 X1, X2 – Connections for a standard 32.768kHz quartz crystal. The internal oscillator circuitry is designed for operation with a crystal having a specified load capacitance (CL) of 12.5pF.&lt;/p&gt;
&lt;p&gt;Description&lt;br /&gt;
The DS1307 Serial Real-Time Clock is a low-power; full binary-coded decimal (BCD) clock/calendar plus 56 bytes of NV SRAM. Address and data are transferred serially via a 2-wire, bi-directional bus. The clock/calendar provides seconds, minutes, hours, day, date, month, and year information. The end of the month date is automatically adjusted for months with fewer than 31 days, including corrections for leap year. The clock operates in either the 24-hour or 12-hour format with AM/PM indicator. The DS1307 has a built-in power sense circuit that detects power failures and automatically switches to the battery supply.&lt;/p&gt;
&lt;p&gt;Operation&lt;br /&gt;
The DS1307 operates as a slave device on the serial bus. Access is obtained by implementing a START condition and providing a device identification code followed by a register address. Subsequent registers can be accessed sequentially until a STOP condition is executed. When VCC falls below 1.25 x VBAT the device terminates an access in progress and resets the device address counter. Inputs to the device will not be recognized at this time to prevent erroneous data from being written to the device from an out of tolerance system. When VCC falls below VBAT the device switches into a low-current battery backup mode. Upon power-up, the device switches from battery to VCC when VCC is greater than VBAT + 0.2V and recognizes inputs when VCC is greater than 1.25 x VBAT.&lt;/p&gt;
&lt;p&gt;2.3.4 Serial Transceiver (MAX232)&lt;/p&gt;
&lt;p&gt;Features&lt;br /&gt;
 High data rate - 250 &lt;a class=&#34;zem_slink&#34; title=&#34;Data rate units&#34; href=&#34;http://en.wikipedia.org/wiki/Data_rate_units&#34; target=&#34;_blank&#34; rel=&#34;wikipedia&#34;&gt;kbits/sec&lt;/a&gt; under load&lt;br /&gt;
 Operate from single +5V power&lt;br /&gt;
 Uses small capacitors: 0.1 µF&lt;/p&gt;
&lt;p&gt;Pin Configurations&lt;/p&gt;
&lt;p&gt;Description&lt;/p&gt;
&lt;p&gt;The DS232A is a dual RS-232 driver/receiver pair that generates RS-232 voltage levels from a single +5- volt power supply. Additional ±12-volt supplies are not needed since the DS232A uses on-board charge pumps to convert the +5-volt supply to ±10 volts. Driver slew rates and data rates are guaranteed up to 250k bits/sec. The DS232A operates with only 0.1 µF charge pump capacitors.&lt;/p&gt;
&lt;p&gt;2.3.4 Serial EEPROM&lt;br /&gt;
Features&lt;br /&gt;
 Internally Organized 1024 x 8 (8K) memory&lt;br /&gt;
 2-wire Serial Interface&lt;br /&gt;
 Bi-directional Data Transfer Protocol&lt;br /&gt;
 Write Protect Pin for Hardware Data Protection&lt;br /&gt;
 8-byte Page (1K, 2K), 16-byte Page (4K, 8K, 16K) Write modes&lt;br /&gt;
 Partial Page Writes are allowed&lt;/p&gt;
&lt;p&gt;Pin Configurations&lt;/p&gt;
&lt;p&gt;Description&lt;br /&gt;
The AT24C08 provides 8192 bits of serial electrically erasable and programmable read-only memory (EEPROM) organized as 1024 words of 8 bits each. The device is optimized for use in many industrial and commercial applications where low-power and low-voltage operation are essential. The AT24C08 is accessed via a 2-wire serial interface.&lt;/p&gt;
&lt;p&gt;2.3.5. Liquid Crystal Display&lt;br /&gt;
Online Temperature Monitoring system uses the Liquid crystal display as the system end display, with the display of 16 x 2. It has the display capability of 16 characters of ASCII data with 2 lines, total of 32 characters&lt;br /&gt;
2.4 About the Software&lt;/p&gt;
&lt;p&gt;2.4.1 Programming Language (ANSI C)&lt;br /&gt;
The software for the project was written in the “C” higher level language, using KEIL compiler.&lt;br /&gt;
Why “C”&lt;br /&gt;
C has become the language of choice for the embedded programmers, because it has the benefit of processor independence. This processor independence allows the programmer to concentrate on algorithms and applications rather than on the details of the processor architecture. However many of its features apply equally to other high level languages as well. Perhaps the strength of the C is that it gives embedded programmers an extraordinary degree of direct hardware control with-out scarifying the benefits of high level languages.&lt;br /&gt;
Compilers and cross-compilers are available for almost every processor with C. Any source code with in C or C++ or Assembly language must be converted it to an executable image that can be loaded into a ROM chip&lt;br /&gt;
2.4.2 KEILCompiler&lt;br /&gt;
Raisonance Integrated Development Environment&lt;br /&gt;
About the KEIL Development Kits&lt;br /&gt;
The Raisonance 8051, XA and ST6 Development Kits are a complete solution to creating software for the 8051 family, XA family and ST6 family of microcontrollers. The Development Kits comprise many different tools that allow projects ranging from simple to highly complex to be developed with relative ease. KEIL has been developing embedded tools since 1988 and has built up many years of experience.&lt;br /&gt;
Development Tools&lt;br /&gt;
• ANSI C Compiler&lt;br /&gt;
The C Compiler is an ANSI compliant compiler that takes source files and generates object files. Extensions to the C language are used to enable features of the microcontroller to be used or controlled.&lt;br /&gt;
• Assembler&lt;br /&gt;
The Assembler takes source files written in assembler and generates object files.&lt;br /&gt;
• Linker/Locator&lt;br /&gt;
The Linker combines the object files generated by the Compiler and Linker and produces a different kind of object file. The Linker also decides where certain types of Data and Code are located in memory.&lt;br /&gt;
• Object-to-HEX Converter&lt;br /&gt;
The converter converts an object file generated by the linker and generates an Intel Hex file, compatible with most device programmers.&lt;br /&gt;
• KEIL&lt;br /&gt;
The KEIL Integrated Development Environment. KEIL is a Windows program that allows the user to create projects, easily call the Compiler, Assembler and Linker to build the project and either simulate or debug the project.&lt;br /&gt;
• Library Manager&lt;br /&gt;
The Library Manager can take object files generated by the Compiler or Assembler and create a library that is included in other projects.&lt;br /&gt;
• Monitor&lt;br /&gt;
The Monitor is a program that runs on hardware and transmits debugging information back to KEIL as the program executes. It also provides a means of controlling the execution of the program and debugging the program while it is executing on hardware&lt;br /&gt;
Development Steps&lt;/p&gt;
&lt;p&gt;1. RIDE provides an editor which allows the user to generate C source files (.c extension) and Assembler source files (.a51 extension for 8051, .axa extension for XA and .st6 extension for ST6).&lt;/p&gt;
&lt;p&gt;2. Each source file is translated using the appropriate tool. The Compiler translates C source files. The Assembler translates assembler source files. Each tool generates a re-locatable object file (.obj extension). If a project has more than one C source file or more than one Assembler source file, then the Compiler and Assembler are executed as many times as required.&lt;/p&gt;
&lt;p&gt;3. If a library file is being generated then the Library Manager takes all the re-locatable object files and combines them into a library file (.lib extension). The library file may then be linked in with other projects.&lt;/p&gt;
&lt;p&gt;4. The Linker/Locator takes relocatable object files and library files and links them together resolving external references. The Linker/Locator then locates variables and code to specific addresses in the memory map. The Linker/Locator generates a single Absolute Object File (.aof extension). It also generates the same file with no extension.&lt;/p&gt;
&lt;p&gt;5. The Absolute Object File may then be used by the simulator or debugger in KEIL, as the file can contain debugging information. Alternatively the Absolute Object File may be used with In-Circuit Emulators.&lt;/p&gt;
&lt;p&gt;6. The Object-HEX Converter tool converts an Absolute Object File into an Intel HEX file (.hex extension) which is a representation of the pure binary code generated, without debugging information. The Intel HEX File is accepted by virtually all device programmers. In addition to being an editor, simulator and debugger, KEIL also controls and automates the entire build process. By selecting a single menu item, KEIL will execute the correct tools to generate either a library file or an Absolute Object File and Intel HEX File.&lt;/p&gt;
&lt;p&gt;Minimum System Requirements&lt;/p&gt;
&lt;p&gt;• Windows 9x/NT/2000&lt;br /&gt;
• Pentium Processor&lt;br /&gt;
• 20Mb Hard disk Space&lt;br /&gt;
• 32 Mb RAM&lt;/p&gt;
&lt;p&gt;2.4.3 Front End Tool&lt;br /&gt;
2.4.4 Platform&lt;/p&gt;
&lt;p&gt;3. Hardware Design &amp;amp; Description&lt;/p&gt;
&lt;p&gt;Fig. 1 Block Diagram of the Online Temperature Monitoring system&lt;/p&gt;
&lt;p&gt;System Description&lt;/p&gt;
&lt;p&gt;. This I2C provides the reliable and faster communication between the master (Microcontroller) device and the other slave (Real Time Clock, Serial EEPROM) devices.&lt;/p&gt;
&lt;p&gt;4. System analysis&lt;/p&gt;
&lt;p&gt;The system analysis task consists of two sub-tasks.&lt;br /&gt;
They are&lt;br /&gt;
1. Data flow diagram&lt;br /&gt;
2. Control flow diagram&lt;/p&gt;
&lt;p&gt;1. Data flow diagram&lt;/p&gt;
&lt;p&gt;A data flow diagram is a graphical representation that depicts information flow and the transforms that are applied as data move from input to output. The DFD may used to represent a system or software at any level of abstraction.&lt;/p&gt;
&lt;p&gt;Key notations used in DFD and CFD&lt;/p&gt;
&lt;p&gt;Context-level DFD for OTMS&lt;/p&gt;
&lt;p&gt;A level 0 DFD also called a fundamental system model or a context model represents the entire software element as a single bubble with input and output data indicated by incoming and outgoing arrows respectively.&lt;br /&gt;
As the DFD is refined into greater levels of details, the analyst performs an implicit functional decomposition of the system, thereby accomplishing the forth operational analysis for function. At the same time, the DFD refinement results in a corresponding refinement of data as it moves through the processes that embody the application.&lt;/p&gt;
&lt;p&gt;Fig. 2 Context Level Data Flow Diagram&lt;/p&gt;
&lt;p&gt;Level 1 DFD for OTMS&lt;/p&gt;
&lt;p&gt;Fig. 3 Level 1 Data Flow Diagram&lt;/p&gt;
&lt;p&gt;Data Flow Information&lt;br /&gt;
1) User commands and data&lt;br /&gt;
2) Configure request&lt;br /&gt;
3) Configure data&lt;br /&gt;
4) Start / Stop&lt;br /&gt;
5) Sensor status&lt;br /&gt;
6) Configuration data&lt;br /&gt;
7) A/d message&lt;br /&gt;
8) Configuration data&lt;br /&gt;
9) Sensor information&lt;br /&gt;
10) Display information&lt;br /&gt;
11) Alarm type&lt;/p&gt;
&lt;p&gt;Level 2 DFD of OTMS&lt;/p&gt;
&lt;p&gt;Level 2 Data Flow Diagram refines the monitor sensors process&lt;/p&gt;
&lt;p&gt;Fig. 1 Level 2 Data Flow Diagram&lt;/p&gt;
&lt;p&gt;Data Flow Information&lt;/p&gt;
&lt;p&gt;1. Sensor status&lt;br /&gt;
2. Sensor ID, type&lt;br /&gt;
3. Alarm data&lt;br /&gt;
4. Configuration data&lt;br /&gt;
5. Sensor ID, type, location&lt;br /&gt;
6. sensor information&lt;br /&gt;
7. Alarm type&lt;/p&gt;
&lt;p&gt;Control flow Diagram&lt;/p&gt;
&lt;p&gt;A large class application are “driven” by events rather than data; produce control information rather than that reports or displays, and process information with heavy concern for time and performance.&lt;/p&gt;
&lt;p&gt;Fig. 1 Control Flow Diagram&lt;br /&gt;
Data Flow Information&lt;/p&gt;
&lt;p&gt;1. User commands and data&lt;br /&gt;
2. Configure request&lt;br /&gt;
3. Configure data&lt;br /&gt;
4. Start / Stop&lt;br /&gt;
5. Sensor status&lt;br /&gt;
6. Configuration data&lt;br /&gt;
7. A/d message&lt;br /&gt;
8. Configuration data&lt;br /&gt;
9. Sensor information&lt;br /&gt;
10. Display information&lt;br /&gt;
11. Alarm type&lt;br /&gt;
5. Software Design&lt;br /&gt;
The software design will includes two parts&lt;br /&gt;
• C Program Design (Source)&lt;br /&gt;
5.1 “C” program design&lt;br /&gt;
This is the main program of Online Temperature Monitoring System. This is compiled with the RIDE (Raisonance Integrated Development Environment) compiler and the generated executable image will be dumped into the Microcontroller (AT 89c52) Chip. The complete Flow Charts for the program are given below&lt;br /&gt;
The main program routine&lt;/p&gt;
&lt;p&gt;Flow Chart for the “Control” Routine&lt;/p&gt;
&lt;p&gt;Flow Chart for the “Write Time” Routine&lt;/p&gt;
&lt;p&gt;1. I2C Protocol&lt;br /&gt;
2. Serial Communication Protocol&lt;br /&gt;
6.2 I2C Protocol Communication&lt;br /&gt;
Overview&lt;br /&gt;
In consumer electronics, telecommunications and industrial electronics, there are often many similarities between seemingly unrelated designs. For example, nearly every system includes: • Some intelligent control, usually a single-chip microcontroller• General-purpose circuits like LCD drivers, remote I/O ports, RAM, EEPROM, or data converters • Application-oriented circuits such as digital tuning and signal processing circuits for radio and video systems, or DTMF generators for telephones with tone dialing. To exploit these similarities to the benefit of both systems designers and equipment manufacturers, as well as to maximize hardware efficiency and circuit simplicity, Philips developed a simple bi-directional 2-wire bus for efficient inter-IC control. This bus is called the Inter IC or I2C-bus. At present, Philips’ IC range includes more than 150 CMOS and bipolar I2C-bus compatible types for performing functions in all three of the previously mentioned categories. All I2C-bus compatible devices incorporate an on-chip interface which allows them to communicate directly with each other via the I2C-bus. This design concept solves the many interfacing problems encountered when designing digital control circuits.&lt;br /&gt;
Features of the I2C-bus&lt;br /&gt;
• Only two bus lines are required; a serial data line (SDA) and a serial clock line (SCL)&lt;br /&gt;
• Each device connected to the bus is software addressable by a unique address and simple master/slave relationships exist at all times; masters can operate as master-transmitters or as master-receivers&lt;br /&gt;
• It’s a true multi-master bus including collision detection and arbitration to prevent data corruption if two or more masters simultaneously initiate data transfer&lt;br /&gt;
• Serial, 8-bit oriented, bi-directional data transfers can be made at up to 100 kbit/s in the Standard-mode, up to 400 kbit/s in the Fast-mode, or up to 3.4 Mbit/s in the High-speed mode&lt;br /&gt;
• On-chip filtering rejects spikes on the bus data line to preserve data integrity&lt;br /&gt;
• The number of ICs that can be connected to the same bus is limited only by a maximum bus capacitance of 400 pF.&lt;/p&gt;
&lt;p&gt;Designer benefits&lt;/p&gt;
&lt;p&gt;• Functional blocks on the block diagram correspond with the actual ICs; designs proceed rapidly from block diagram to final schematic.&lt;br /&gt;
• No need to design bus interfaces because the I2C-bus interface is already integrated on-chip.&lt;br /&gt;
• Integrated addressing and data-transfer protocol allow systems to be completely software-defined&lt;br /&gt;
• The same IC types can often be used in many different applications&lt;br /&gt;
• Design-time reduces as designers quickly become familiar with the frequently used functional blocks represented by I2C-bus compatible ICs&lt;br /&gt;
• ICs can be added to or removed from a system without affecting any other circuits on the bus&lt;br /&gt;
• Fault diagnosis and debugging are simple; malfunctions can be immediately traced&lt;/p&gt;
&lt;p&gt;The I2C-Bus Concept&lt;br /&gt;
The I2C-bus contains two wires, serial data (SDA) and serial clock (SCL), carry information between the devices connected to the bus. Each device is recognized by a unique address (whether it’s a microcontroller, LCD driver, memory or keyboard interface) and can operate as either a transmitter or receiver, depending on the function of the device. Obviously an LCD driver is only a receiver, whereas a memory can both receive and transmit data. In addition to transmitters and receivers, devices can also be considered as masters or slaves when performing data transfers&lt;/p&gt;
&lt;p&gt;Definition of I2C-bus Terminology&lt;/p&gt;
&lt;p&gt;Byte format&lt;br /&gt;
Every byte put on the SDA line must be 8-bits long. The number of bytes that can be transmitted per transfer is unrestricted. Each byte has to be followed by an acknowledge bit. Data is transferred with the most significant bit (MSB) first (see Fig. a). If a slave can’t receive or transmit another complete byte of data until it has performed some other function, for example servicing an internal interrupt, it can hold the clock line SCL LOW to force the master into a wait state. Data transfer then continues when the slave is ready for another byte of data and releases clock line SCL.&lt;/p&gt;
&lt;p&gt;Fig a. Data transfer on I2C Bus&lt;/p&gt;
&lt;p&gt;Acknowledge&lt;br /&gt;
Data transfer with acknowledge is obligatory. The acknowledge-related clock pulse is generated by the master. The transmitter releases the SDA line (HIGH) during the acknowledge clock pulse. The receiver must pull down the SDA line during the acknowledge clock pulse so that it remains stable LOW during the HIGH period of this clock pulse (see Fig.).&lt;/p&gt;
&lt;p&gt;Fig b. Acknowledge on I2C Bus&lt;/p&gt;
&lt;p&gt;Formats with 7-Bit Addresses&lt;br /&gt;
Data transfers follow the format shown in Fig.a After the START condition (S), a slave address is sent. This address is 7 bits long followed by an eighth bit which is a data direction bit (R/W) - a ‘zero’ indicates a transmission (WRITE), a ‘one’ indicates a request for data (READ). A data transfer is always terminated by a STOP condition (P) generated by the master. However, if a master still wishes to communicate on the bus, it can generate a repeated START condition (Sr) and address another slave without first generating a STOP condition. Various combinations of read/write formats are then possible within such a transfer.&lt;/p&gt;
&lt;p&gt;Fig. a. complete data transfer.&lt;/p&gt;
&lt;p&gt;Complete Data Transfer Format&lt;/p&gt;
&lt;p&gt;S- START Condition&lt;br /&gt;
P- STOP Condition&lt;br /&gt;
A- Acknowledgement&lt;/p&gt;
&lt;p&gt;3. Serial Communication Protocol&lt;br /&gt;
The establishment of RS 232 and ASCCII coined with the development of multi-user computer organizations wherein a number of users of users were linked to a host mainframe via serial data links, and serial data was encoded in ASCII. Peripheral devices, such as printers, adopted the same standards in order to access the growing market for serial devices.&lt;br /&gt;
Serial data transmission using ASCII become so universal that specialized integrated circuits, universal Asynchronous Receiver Transmitters (UARTS), were developed to perform the tasks of converting an 8-bit parallel data byte to a 10-bit serial stream and converting 10-bit serial stream to an 8-bit parallel byte.&lt;br /&gt;
The 89c52 contains serial data transmission / reception circuitry that can be programmed to use four asynchronous data communication modes numbered from 0 to 3.&lt;/p&gt;
&lt;p&gt;• Mode 0: High-Speed, 8-bit shift register; one baud rate of f/12&lt;br /&gt;
• Mode 1: Standard 8-bit UART; variable baud rates&lt;br /&gt;
• Mode 2 &amp;amp; Mode3 Multiprocessor 9-bit UART&lt;/p&gt;
&lt;p&gt;Asynchronous 8-bit Character (Mode1)&lt;/p&gt;
&lt;p&gt;7. Operational Specifications&lt;/p&gt;
&lt;p&gt;8. Application Real Time&lt;/p&gt;
&lt;p&gt;9. Conclusion&lt;br /&gt;
9.1 Future Scope&lt;/p&gt;
&lt;p&gt;9.2 Limitations&lt;br /&gt;
.&lt;/p&gt;
&lt;p&gt;10. Bibliography&lt;/p&gt;
</description>
        </item>
      
    
      
        <item>
          <title>One liner: To get available virtual memory</title>
          <link>https://shafiqissani.github.io/www.shafiq.in/2011/12/10/one-liner-to-get-available-virtual-memory/</link>
          <pubDate>Sat, 10 Dec 2011 18:13:02 UTC</pubDate>
          <author></author>
          <guid>https://shafiqissani.github.io/www.shafiq.in/2011/12/10/one-liner-to-get-available-virtual-memory/</guid>
          <description>&lt;pre&gt;vmstat -s -SM | grep &#34;free memory&#34; | awk -F&#34; &#34; &#39;{print$1}&#39;&lt;/pre&gt;
</description>
        </item>
      
    
      
        <item>
          <title>Descriptive programming in QTP</title>
          <link>https://shafiqissani.github.io/www.shafiq.in/2011/11/11/descriptive-programming-in-qtp/</link>
          <pubDate>Fri, 11 Nov 2011 10:30:05 UTC</pubDate>
          <author></author>
          <guid>https://shafiqissani.github.io/www.shafiq.in/2011/11/11/descriptive-programming-in-qtp/</guid>
          <description>&lt;pre&gt;Download link : &lt;a href=&#34;http://shafiqissani.files.wordpress.com/2011/11/6010dae508eb401983bcc812c77ecb17.doc&#34;&gt;6010dae508eb401983bcc812c77ecb17&lt;/a&gt;&lt;/pre&gt;
&lt;h2&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;&lt;br /&gt;
Introduction:&lt;/span&gt;&lt;/span&gt;&lt;/h2&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;This document demonstrates the usage of Descriptive programming in QTP 8.20. It also discusses situations where Descriptive programming can be used. Using Descriptive Programming automation scripts can be created even if the application has not been developed.&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;h2&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Descriptive Programming:&lt;/span&gt;&lt;/span&gt;&lt;/h2&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Whenever QTP records any action on any object of an application, it adds some description on how to recognize that object to a repository of objects called object repository. QTP cannot take action on an object until unless its object description is in the Object Repository. But descriptive programming provides a way to perform action on objects which are not in Object repository&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#000066;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Object Identification:&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;To identify an object during the play back of the scripts QTP stores some properties which helps QTP to uniquely identify the object on a page. Below screen shots shows an example Object repository:&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Now to recognize a radio button on a page QTP had added 2 properties the name of the radio button and the html tag for it. The name the left tree view is the logical name given by QTP for the object. This can be changed as per the convenience of the person writing the test case. QTP only allows UNIQUE logical name under same level of hierarchy. As we see in the snapshot the two objects in Browser-&amp;gt;Page node are “WebTable” and “testPath”, they cannot have the same logical name. But an object under some other node can have the same name. Now with the current repository that we have, we can only write operation on objects which are in the repository. Some of the example operations are given below&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Browser(&#34;Browser&#34;).Page(&#34;Page&#34;).WebRadioGroup (&#34;testPath&#34;).Select &#34;2&#34;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;cellData = Browser(&#34;Browser&#34;).Page(&#34;Page&#34;).WebTable (&#34;WebTable&#34;).GetCellData (1,1)&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Browser(&#34;Example2&#34;).Page(&#34;Page&#34;).WebEdit(&#34;testPath&#34;).Set &#34;Test text&#34;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#000066;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;When and Why to use Descriptive programming?&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Below are some of the situations when Descriptive Programming can be considered useful: &lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;The objects in the application are dynamic in nature and need special handling to identify the object. The best example would be of clicking a link which changes according to the user of the application, Ex. “Logout &amp;lt;&amp;gt;”.&lt;/span&gt;&lt;/span&gt;&lt;/li&gt;
&lt;li&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;When object repository is getting huge due to the no. of objects being added. If the size of Object repository increases too much then it decreases the performance of QTP while recognizing a object.&lt;/span&gt;&lt;/span&gt;&lt;/li&gt;
&lt;li&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;When you don’t want to use object repository at all. Well the first question would be why not Object repository? Consider the following scenario which would help understand why not Object repository&lt;/span&gt;&lt;/span&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Scenario 1: Suppose we have a web application that has not been developed yet. Now QTP for recording the script and adding the objects to repository needs the application to be up, that would mean waiting for the application to be deployed before we can start of with making QTP scripts. But if we know the descriptions of the objects that will be created then we can still start off with the script writing for testing&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Scenario 2: Suppose an application has 3 navigation buttons on each and every page. Let the buttons be “Cancel”, “Back” and “Next”. Now recording action on these buttons would add 3 objects per page in the repository. For a 10 page flow this would mean 30 objects which could have been represented just by using 3 objects. So instead of adding these 30 objects to the repository we can just write 3 descriptions for the object and use it on any page.&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;ol start=&#34;4&#34;&gt;
&lt;li&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Modification to a test case is needed but the Object repository for the same is Read only or in shared mode i.e. changes may affect other scripts as well. &lt;/span&gt;&lt;/span&gt;&lt;/li&gt;
&lt;li&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;When you want to take action on similar type of object i.e. suppose we have 20 textboxes on the page and there names are in the form txt_1, txt_2, txt_3 and so on. Now adding all 20 the Object repository would not be a good programming approach.&lt;/span&gt;&lt;/span&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;span style=&#34;color:#000066;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;How to use Descriptive programming?&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;There are two ways in which descriptive programming can be used&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;By creating properties collection object for the description.&lt;/span&gt;&lt;/span&gt;&lt;/li&gt;
&lt;li&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;By giving the description in form of the string arguments.&lt;/span&gt;&lt;/span&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;ol&gt;
&lt;li&gt;&lt;span style=&#34;color:#000066;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;By creating properties collection object for the description.&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;To use this method you need first to create an empty description&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Dim&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_Desc&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#339966;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; ‘&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#339966;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Not&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#339966;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;necessary&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#339966;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;to&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#339966;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;declare&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Set obj_Desc = Description.Create&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Now we have a blank description in “obj_Desc”. Each description has 3 properties “Name”, “Value” and “Regular Expression”.&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_Desc(“html tag”).value= “INPUT”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;When you use a property name for the first time the property is added to the collection and when you use it again the property is modified. By default each property that is defined is a regular expression. Suppose if we have the following description&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_Desc(“html tag”).value= “INPUT”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_Desc(“name”).value= “txt.*”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;This would mean an object with html tag as INPUT and name starting with txt. Now actually that “.*” was considered as regular expression. So, if you want the property “name” not to be recognized as a regular expression then you need to set the “regularexpression” property as FALSE&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_Desc(“html tag”).value= “INPUT”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_Desc(“name”).value= “txt.*”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_Desc(“name”).regularexpression= “txt.*”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;This is how of we create a description. Now below is the way we can use it&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Browser(“Browser”).Page(“Page”).WebEdit(obj_Desc).set “Test”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;When we say .WebEdit(obj_Desc) we define one more property for our description that was not earlier defined that is it’s a text box (because QTPs WebEdit boxes map to text boxes in a web page).&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;If we know that we have more than 1 element with same description on the page then we must define “index” property for the that description&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Consider the HTML code given below&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;&amp;lt;INPUT type=”textbox” name=”txt_Name”&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;&amp;lt;INPUT type=”textbox” name=”txt_Name”&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Now the html code has two objects with same description. So distinguish between these 2 objects we will use the “index” property. Here is the description for both the object&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;For&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;1&lt;/span&gt;&lt;/span&gt;&lt;sup&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;st&lt;/span&gt;&lt;/span&gt;&lt;/sup&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;textbox:&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_Desc(&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;“&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;html&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;tag&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;).value=&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; “&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;INPUT&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_Desc(“name”).value= “txt_Name”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_Desc(“index”).value= “0”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;For&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;2&lt;/span&gt;&lt;/span&gt;&lt;sup&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;nd&lt;/span&gt;&lt;/span&gt;&lt;/sup&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;textbox:&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_Desc(&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;“&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;html&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;tag&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;).value=&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; “&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;INPUT&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_Desc(“name”).value= “txt_Name”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_Desc(“index”).value= “1”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Consider the HTML Code given below:&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;&amp;lt;INPUT type=”textbox” name=”txt_Name”&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;&amp;lt;INPUT type=”radio” name=”txt_Name”&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;We can use the same description for both the objects and still distinguish between both of them&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_Desc(“html tag”).value= “INPUT”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_Desc(“name”).value= “txt_Name”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;When I want to refer to the textbox then I will use the inside a WebEdit object and to refer to the radio button I will use the description object with the WebRadioGroup object.&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Browser(&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;“&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Browser&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;).Page(&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;“&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Page&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;).WebEdit(obj_Desc).set&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; “&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Test&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;” &lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;‘&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Refers&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;to&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;the&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;text&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;box&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Browser(&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;“&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Browser&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;).Page(&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;“&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Page&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;).WebRadioGroup(obj_Desc).set&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; “&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Test&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;” &lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;‘&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Refers&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;to&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;the&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;radio&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;button&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;But if we use WebElement object for the description then we must define the “index” property because for a webelement the current description would return two objects.&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#000066;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Hierarchy of test description:&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;When using programmatic descriptions from a specific point within a test object hierarchy, you must continue to use programmatic descriptions&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;from that point onward within the same statement. If you specify a test object by its object repository name after other objects in the hierarchy have&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;been described using programmatic descriptions, QuickTest cannot identify the object.&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;For example, you can use Browser(Desc1).Page(Desc1).Link(desc3), since it uses programmatic descriptions throughout the entire test object hierarchy.&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;You can also use Browser(&#34;Index&#34;).Page(Desc1).Link(desc3), since it uses programmatic descriptions from a certain point in the description (starting&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;from the Page object description).&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;However, you cannot use Browser(Desc1).Page(Desc1).Link(&#34;Example1&#34;), since it uses programmatic descriptions for the Browser and Page objects but&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;then attempts to use an object repository name for the Link test object (QuickTest tries to locate the Link object based on its name, but cannot&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;locate it in the repository because the parent objects were specified using programmatic descriptions).&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#000066;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Getting Child Object:&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;We can use description object to get all the objects on the page that matches that specific description. Suppose we have to check all the checkboxes present on a web page. So we will first create an object description for a checkboxe and then get all the checkboxes from the page&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Dim obj_ChkDesc&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Set obj_ChkDesc=Description.Create&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_ChkDesc(“html tag”).value = “INPUT”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_ChkDesc(“type”).value = “checkbox”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Dim allCheckboxes, singleCheckBox&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Set allCheckboxes = Browse(“Browser”).Page(“Page”).ChildObjects(obj_ChkDesc)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;For each singleCheckBox in allCheckboxes&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; singleCheckBox.Set “ON”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Next&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;The above code will check all the check boxes present on the page. To get all the child objects we need to specify an object description i.e. we can’t use the string arguments that will be discussed later in the 2nd way of using the programming description.&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#000066;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Possible Operation on Description Object&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Consider the below code for all the solutions&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Dim obj_ChkDesc&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Set obj_ChkDesc=Description.Create&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_ChkDesc(“html tag”).value = “INPUT”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_ChkDesc(“type”).value = “checkbox”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Q: How to get the no. of description defined in a collection&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;A:&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_ChkDesc.Count&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;‘&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Will&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;return&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;2&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;in&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;our&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;case&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Q: How to remove a description from the collection&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;A:&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_ChkDesc.remove&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; “&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;html&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;tag&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;‘&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;would&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;delete&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;the&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;html&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;tag&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;property&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;from&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;the&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;collection&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Q: How do I check if property exists or not in the collection?&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;A: The answer is that it’s not possible. Because whenever we try to access a property which is not defined its automatically added to the collection. The only way to determine is to check its value that is use a if statement “if obj_ChkDesc(“html tag”).value = empty then”.&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Q: How to browse through all the properties of a properties collection?&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;A: Two ways&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; 1&lt;/span&gt;&lt;/span&gt;&lt;sup&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;st&lt;/span&gt;&lt;/span&gt;&lt;/sup&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;:&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;For&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;each&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;desc&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;in&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_ChkDesc&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; Name=desc.Name&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; Value=desc.Value&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; RE = desc.regularexpression&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; Next&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; 2&lt;/span&gt;&lt;/span&gt;&lt;sup&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;nd&lt;/span&gt;&lt;/span&gt;&lt;/sup&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;:&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;For&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;i=0&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;to&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;obj_ChkDesc.count&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;-&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;1&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; Name= obj_ChkDesc(i).Name&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; Value= obj_ChkDesc(i).Value&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; RE = obj_ChkDesc(i).regularexpression&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; Next&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;ol start=&#34;2&#34;&gt;
&lt;li&gt;&lt;span style=&#34;color:#000066;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;By giving the description in form of the string arguments.&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;You&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;can&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;describe&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;an&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;object&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;directly&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;in&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;a&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;statement&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;by&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;specifying&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;&lt;em&gt;property:=value&lt;/em&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;pairs&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;describing&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;the&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;object&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;instead&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;of&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;specifying&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;an&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;object&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;’&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;s&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;name. The general syntax is:&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;TestObject(&#34;PropertyName1:=PropertyValue1&#34;, &#34;...&#34; , &#34;PropertyNameX:=PropertyValueX&#34;)&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;TestObject—the test object class could be WebEdit, WebRadioGroup etc….&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;PropertyName:=PropertyValue&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;—&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;the&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;test&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;object&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;property&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;and&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;its&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;value.&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Each&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;&lt;em&gt;property:=value&lt;/em&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;pair&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;should&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;be&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;separated&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;by&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;commas&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;and&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;quotation&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;marks. Note that you can enter a variable name as the property value if you want to find an object based on property values you retrieve during a run session.&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Consider the HTML Code given below:&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;&amp;lt;INPUT type=”textbox” name=”txt_Name”&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;&amp;lt;INPUT type=”radio” name=”txt_Name”&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Now to refer to the textbox the statement would be as given below&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Browser(“Browser”).Page(“Page”).WebEdit(“Name:=txt_Name”,”html tag:=INPUT”).set “Test”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;And to refer to the radio button the statement would be as given below&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Browser(“Browser”).Page(“Page”).WebRadioGroup(“Name:=txt_Name”,”html tag:=INPUT”).set “Test”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;If we refer to them as a web element then we will have to distinguish between the 2 using the index property&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Browser(&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;“&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Browser&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;).Page(&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;“&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Page&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;).WebElement(&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;“&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Name:=txt_Name&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;,&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;html&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;tag:=INPUT&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;,&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Index:=0&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;).set&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; “&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Test&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;” &lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;‘ &lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Refers&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;to&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;the&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;textbox&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Browser(&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;“&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Browser&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;).Page(&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;“&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Page&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;).WebElement(&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;“&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Name:=txt_Name&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;,&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;html&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;tag:=INPUT&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;,&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Index:=1&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;).set&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt; “&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Test&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#0000ff;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;” &lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;‘ &lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Refers&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;to&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;the&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;radio&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;color:#008000;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;button&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;&lt;span style=&#34;color:#000066;&#34;&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Reference:&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;“&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Mercury&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;QuickTest&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Professional,&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;User&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;’&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;s&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Guide,&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;Version&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;8.0.1&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;font-family:Times, &#39;Times New Roman&#39;, serif;&#34;&gt;&lt;span style=&#34;font-size:small;&#34;&gt;”&lt;/span&gt;&lt;/span&gt;&lt;/li&gt;
&lt;/ol&gt;
</description>
        </item>
      
    
      
        <item>
          <title>College Website - My first project !</title>
          <link>https://shafiqissani.github.io/www.shafiq.in/2011/06/15/college-website-my-first-project/</link>
          <pubDate>Wed, 15 Jun 2011 03:43:37 UTC</pubDate>
          <author></author>
          <guid>https://shafiqissani.github.io/www.shafiq.in/2011/06/15/college-website-my-first-project/</guid>
          <description>&lt;p&gt;I had mentioned earlier on twitter that I will be uploading some recovered data from my broken hard drive. So here is the first one.&lt;/p&gt;
&lt;p&gt;Following is the link to a self extracting archive of the first project I ever did. The college website! ...with help of the Subhash Dasyam.&lt;/p&gt;
&lt;pre&gt;&lt;a href=&#34;https://github.com/shafiqissani/ASTRA-College-Website&#34;&gt;https://github.com/shafiqissani/ASTRA-College-Website&lt;/a&gt;&lt;/pre&gt;
&lt;p&gt;This is a very very very old project. The structure of the website and code is horrible and outdated.&lt;/p&gt;
&lt;p&gt;Make sure you scan this for virus. Most systems in our college were compromised. Also there should be a php shell in there somewhere ... we used it for administrative purposes only.  :)&lt;/p&gt;
&lt;p&gt;A thank you shout out to Mrs. Aruna Ranganath (CSE Head of Department) for getting me involved.&lt;/p&gt;
&lt;p&gt;Ahhh... I feel nostalgic.  :)&lt;/p&gt;
</description>
        </item>
      
    
      
        <item>
          <title>Notes on build script</title>
          <link>https://shafiqissani.github.io/www.shafiq.in/2011/05/10/notes-on-build-script/</link>
          <pubDate>Tue, 10 May 2011 00:01:40 UTC</pubDate>
          <author></author>
          <guid>https://shafiqissani.github.io/www.shafiq.in/2011/05/10/notes-on-build-script/</guid>
          <description>&lt;ul&gt;
&lt;li&gt;Choose a language that can be used in &lt;a class=&#34;zem_slink&#34; title=&#34;Cross-platform&#34; href=&#34;http://en.wikipedia.org/wiki/Cross-platform&#34; rel=&#34;wikipedia&#34;&gt;multiple platforms&lt;/a&gt; (very important for a &lt;a class=&#34;zem_slink&#34; title=&#34;Java (programming language)&#34; href=&#34;http://www.oracle.com/technetwork/java/&#34; rel=&#34;homepage&#34;&gt;Java&lt;/a&gt; project)&lt;/li&gt;
&lt;li&gt;Ensure that as many steps are automated. Begin with a full clean up and allow full &lt;a class=&#34;zem_slink&#34; title=&#34;Software build&#34; href=&#34;http://en.wikipedia.org/wiki/Software_build&#34; rel=&#34;wikipedia&#34;&gt;build&lt;/a&gt; and deployment to the server&lt;/li&gt;
&lt;li&gt;&lt;a class=&#34;zem_slink&#34; title=&#34;Automation&#34; href=&#34;http://en.wikipedia.org/wiki/Automation&#34; rel=&#34;wikipedia&#34;&gt;Automate&lt;/a&gt; even server restart to increase developer efficiency&lt;/li&gt;
&lt;li&gt;Each build should ensure that it recompiles all classes and optionally fetches latest code from the repository&lt;/li&gt;
&lt;li&gt;Stale references to others code is the most frequent offense among developers. So use the build script to enforce some discipline too&lt;/li&gt;
&lt;li&gt;While writing a build script, one needs to take care of small short cuts that can help in a faster build and deployment&lt;/li&gt;
&lt;/ul&gt;
&lt;ul&gt;
&lt;/ul&gt;
&lt;ul&gt;
&lt;li&gt;Deploy only &lt;a class=&#34;zem_slink&#34; title=&#34;HTML&#34; href=&#34;http://en.wikipedia.org/wiki/HTML&#34; rel=&#34;wikipedia&#34;&gt;HTML&lt;/a&gt; and &lt;a class=&#34;zem_slink&#34; title=&#34;JavaServer Pages&#34; href=&#34;http://en.wikipedia.org/wiki/JavaServer_Pages&#34; rel=&#34;wikipedia&#34;&gt;JSP&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;Deploy only compiled java classes&lt;/li&gt;
&lt;li&gt;Do only a restart&lt;/li&gt;
&lt;li&gt;Deploy only properties files&lt;/li&gt;
&lt;/ul&gt;
&lt;li&gt;This is not a default option and one should not spend time at this in the beginning. This is a step more relevant to iterative improvement of the build script&lt;/li&gt;
</description>
        </item>
      
    
      
        <item>
          <title>Puppet Error - Could not file class in namespace - [solved]</title>
          <link>https://shafiqissani.github.io/www.shafiq.in/2011/01/18/puppet-error-could-not-file-class-in-namespace-solved/</link>
          <pubDate>Tue, 18 Jan 2011 12:33:39 UTC</pubDate>
          <author></author>
          <guid>https://shafiqissani.github.io/www.shafiq.in/2011/01/18/puppet-error-could-not-file-class-in-namespace-solved/</guid>
          <description>&lt;p&gt;Yet another silly &lt;a class=&#34;zem_slink&#34; title=&#34;Error message&#34; rel=&#34;wikipedia&#34; href=&#34;http://en.wikipedia.org/wiki/Error_message&#34;&gt;error message&lt;/a&gt;... that is nowhere near accurate. So if you get the following error one of the possible mistakes would be a missing curly bracket. Yea, I wasted an hour on this one. ppsshh&lt;/p&gt;
&lt;pre&gt;err: Could not retrieve catalog: Could not find class php in namespaces standardbuild at /etc/puppet/manifests/templates.pp:15 on domain.internal.com&lt;/pre&gt;
</description>
        </item>
      
    
      
        <item>
          <title>ERROR: `phpize&#39; failed [solved]</title>
          <link>https://shafiqissani.github.io/www.shafiq.in/2011/01/17/error-phpize-failed-solved/</link>
          <pubDate>Mon, 17 Jan 2011 12:21:05 UTC</pubDate>
          <author></author>
          <guid>https://shafiqissani.github.io/www.shafiq.in/2011/01/17/error-phpize-failed-solved/</guid>
          <description>&lt;p&gt;If you get the following error install the development files of &lt;a class=&#34;zem_slink&#34; title=&#34;PHP&#34; rel=&#34;homepage&#34; href=&#34;http://www.php.net/&#34;&gt;PHP&lt;/a&gt;.&lt;/p&gt;
&lt;pre&gt;running: &lt;a class=&#34;zem_slink&#34; title=&#34;PHPize&#34; rel=&#34;homepage&#34; href=&#34;http://www.phpize.com&#34;&gt;phpize&lt;/a&gt;
sh: phpize: not found
ERROR: `phpize&#39; failed&lt;/pre&gt;
&lt;p&gt;On Ubuntu/Debian you could run the following line in the terminal :&lt;/p&gt;
&lt;pre&gt;&lt;a class=&#34;zem_slink&#34; title=&#34;Advanced Packaging Tool&#34; rel=&#34;homepage&#34; href=&#34;http://wiki.debian.org/Apt&#34;&gt;apt-get&lt;/a&gt; install php5-dev&lt;/pre&gt;
&lt;p&gt;&lt;span style=&#34;font-family:Georgia, &#39;Times New Roman&#39;, &#39;Bitstream Charter&#39;, Times, serif;line-height:19px;white-space:normal;font-size:13px;&#34;&gt;that&#39;s it :)&lt;/span&gt;&lt;/p&gt;
</description>
        </item>
      
    
      
        <item>
          <title>Perl - system load</title>
          <link>https://shafiqissani.github.io/www.shafiq.in/2010/09/07/perl-system-load/</link>
          <pubDate>Tue, 07 Sep 2010 14:15:06 UTC</pubDate>
          <author></author>
          <guid>https://shafiqissani.github.io/www.shafiq.in/2010/09/07/perl-system-load/</guid>
          <description>&lt;p&gt;To find the system load use the following perl snippet :&lt;/p&gt;
&lt;p&gt;1) &lt;a class=&#34;zem_slink&#34; title=&#34;Load (computing)&#34; rel=&#34;wikipedia&#34; href=&#34;http://en.wikipedia.org/wiki/Load_%28computing%29&#34;&gt;System load&lt;/a&gt; of last one minute :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;&lt;a class=&#34;zem_slink&#34; title=&#34;Uptime&#34; rel=&#34;wikipedia&#34; href=&#34;http://en.wikipedia.org/wiki/Uptime&#34;&gt;uptime&lt;/a&gt; | &lt;a class=&#34;zem_slink&#34; title=&#34;AWK&#34; rel=&#34;homepage&#34; href=&#34;http://cm.bell-labs.com/cm/cs/awkbook/index.html&#34;&gt;awk&lt;/a&gt; -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-1&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f1&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;2) System load of last 5 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
{ print $2 }
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
{ print $2 }
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre wp-pre-tag-2&gt;&lt;/pre&gt;
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
 | cut -d, -f2&#39;);&lt;/span&gt;&lt;/pre&gt;
&lt;p&gt;3) System load of last 15 minutes :&lt;/p&gt;
&lt;pre&gt;&lt;del&gt;&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = exec(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/del&gt;
&lt;span style=&#34;font-family:Georgia, &#39;Bitstream Charter&#39;, serif;color:#444444;&#34;&gt;my $system_load = qx(&#39;uptime | awk -F &#34;load average: &#34; 
{ print $2 }
 | cut -d, -f3&#39;);&lt;/span&gt;&lt;/pre&gt;
</description>
        </item>
      
    
      
        <item>
          <title>Gearman - Can&#39;t call method &#34;syswrite&#34; on an undefined value at /usr/local/share/perl/5.10.1/Gearman/Taskset.pm line 202.</title>
          <link>https://shafiqissani.github.io/www.shafiq.in/2010/08/31/gearman-cant-call-method-syswrite-on-an-undefined-value-at-usrlocalshareperl5-10-1gearmantaskset-pm-line-202/</link>
          <pubDate>Tue, 31 Aug 2010 15:07:09 UTC</pubDate>
          <author></author>
          <guid>https://shafiqissani.github.io/www.shafiq.in/2010/08/31/gearman-cant-call-method-syswrite-on-an-undefined-value-at-usrlocalshareperl5-10-1gearmantaskset-pm-line-202/</guid>
          <description>&lt;p&gt;If you get the following error while running the client code :&lt;/p&gt;
&lt;p&gt;Can&#39;t call method &#34;syswrite&#34; on an undefined value at /usr/local/share/perl/5.10.1/Gearman/Taskset.pm line 202.&lt;/p&gt;
&lt;p&gt;... then change this&lt;/p&gt;
&lt;div id=&#34;_mcePaste&#34;&gt;$client-&amp;gt;job_servers(&#39;127.0.0.1&#39;);&lt;/div&gt;
&lt;div&gt;to&lt;/div&gt;
&lt;div&gt;&lt;span style=&#34;line-height:23px;font-size:14px;&#34;&gt;$client-&amp;gt;job_servers(&#39;127.0.0.1:4730&#39;);&lt;/span&gt;&lt;/div&gt;
&lt;div&gt;&lt;span style=&#34;line-height:23px;font-size:14px;&#34;&gt;thats it ! &lt;/span&gt;&lt;/div&gt;
&lt;div&gt;&lt;span style=&#34;line-height:23px;font-size:14px;&#34;&gt;:)&lt;/span&gt;&lt;/div&gt;
</description>
        </item>
      
    

  </channel>
</rss>
